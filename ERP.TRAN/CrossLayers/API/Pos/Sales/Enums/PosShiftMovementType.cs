namespace ERP.TRAN.CrossLayers.API.Pos.Sales.Enums;

public enum PosShiftMovementType
{

    Income = 1,   // Ingreso / Adición de efectivo (ej. más cambio)
    Expense = 2   // Salida / Retiro de efectivo (ej. gasto menor)
}